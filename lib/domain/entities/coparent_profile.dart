/// Profil minimal du co-parent, tel que visible une fois appairé.
class CoparentProfile {
  final String id;
  final String firstName;

  const CoparentProfile({required this.id, required this.firstName});
}
